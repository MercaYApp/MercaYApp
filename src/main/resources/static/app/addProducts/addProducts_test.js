'use strict';

describe('myApp.addProducts module', function() {

  beforeEach(module('myApp.addProducts'));

  describe('addProducts controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewAddProductosCtrl = $controller('ViewAddProductosCtrl');
      expect(viewAddProductosCtrl).toBeDefined();
    }));

  });
});