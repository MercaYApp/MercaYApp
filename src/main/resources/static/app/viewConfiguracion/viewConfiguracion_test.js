'use strict';

describe('myApp.viewConfiguracion module', function() {

  beforeEach(module('myApp.viewConfiguracion'));

  describe('viewConfiguracion controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewLoginCtrl = $controller('ViewConfiguracionController');
      expect(viewLoginCtrl).toBeDefined();
    }));

  });
});